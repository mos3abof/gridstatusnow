home_page:
    pattern:  /
    defaults: { _controller: PowerGridBundle:Default:index }

status:
    pattern:  /status
    defaults: { _controller: PowerGridBundle:Default:status }

history:
    pattern:  /history
    defaults: { _controller: PowerGridBundle:Default:history }


history_archive:
    pattern: /history/{month_name}
    defaults: { _controller: PowerGridBundle:Default:history }


history_archive_month_and_year:
    pattern: /history/{month_name}/{year}
    defaults: { _controller: PowerGridBundle:Default:history }

api:
    pattern: /api
    defaults: { _controller: PowerGridBundle:Default:api}

about:
    pattern: /about
    defaults: { _controller: PowerGridBundle:Default:about}

contact:
    pattern: /contact
    defaults: { _controller: PowerGridBundle:Default:contact}

faq:
    pattern: /faq
    defaults: { _controller: PowerGridBundle:Default:faq}

# power_grid:
    # resource: "@PowerGridBundle/Resources/config/routing.yml"
    # prefix:   /
#
